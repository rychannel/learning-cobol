# How to compile a COBOL program
`cobc -x <program-file>.cob`
# How to run a COBOL program
`./<compiled-program>`

# How to compile to byte code
`cobc -m <program-file>.cob`

# How to run bytecode
`cobcrun <program-file>.so`